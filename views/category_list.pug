extends layout

block content
  h1=title

  if category_list.length
    ul
      for category in category_list
        li
          a(href=category.url) #{category.name}
    
  else
    p No category added yet. 
      a(href="/category/create") Create a new category
      | .